package com.kodilla.collections.interfaces.homework;

public interface Car {
    int increaseSpeed();
    int decreaseSpeed(); /* W zadaniu należało stworzyć increaseSpeed i decreaseSpeed, ale nigdzie nie zostały użyte.
    Automatycznie utworzyły się w klasach aut na podstawie tego interfejsu. */
    int getSpeed();
    String getName();
}
